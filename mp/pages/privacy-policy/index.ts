/**
 * 隐私政策页(占位)
 *
 * 内容由法务起草;ce-work 阶段替换 PRIVACY_POLICY_BODY 为审核通过的正式文案。
 */

const PRIVACY_POLICY_BODY = `炎炎消防队隐私政策(占位文本,正式发布前由法务审核替换)

数据采集范围:
1. 健康生理信息 — 仅在你授权微信运动后读取每日步数,精度日级。
2. 医疗体检数据 — 你主动上传体检报告时,系统 OCR 解析血糖/尿酸/CRP/血脂/鼻炎记录用于长程改善验证;原始报告图片存储于阿里云华东 OSS,30 天后自动删除。
3. 食物照片 — 你拍摄的餐食照片送入境内多模态模型(豆包/Qwen-VL)识别,识别完成后图片仅在你的账号下保留;不出境、不用于训练第三方模型。
4. 所在城市 — 仅城市级,用于查询当地 PM2.5 与花粉指数。
5. 推送 — 用于 7:30 次晨打卡提醒,可在设置中关闭。

存储与加密:
- 全部数据存储于阿里云华东 region。
- 敏感字段(食物识别结果、症状打卡)在数据库层做应用层 AES-256-GCM 加密(envelope encryption),你的数据加密密钥(DEK)由阿里云 KMS 主密钥保护。

撤回与删除:
- 你可随时在「我的 → 撤回同意」立即撤回;撤回触发 KMS 立即吊销 DEK 解密权限,即使后台仍持有数据,任何尝试解密都会被拒绝。
- 撤回 30 天后,所有数据将被永久硬删除,DEK 被销毁。

详见正式版本(法务审核后发布)。`;

Page<{ body: string }, { onCopyTap: () => void }>({
  data: { body: PRIVACY_POLICY_BODY },

  onCopyTap() {
    wx.setClipboardData({ data: this.data.body });
  }
});
